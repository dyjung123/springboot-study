mybatis:
  configuration:
    map-underscore-to-camel-case: true

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/spring_study?characterEncoding=UTF-8&serverTimezone=Asia/Tokyo
    username: dongryun
    password: password
  flyway:
    locations: classpath:db/migration
  devtools:
    remote:
      restart:
        enabled: true
    livereload:
      enabled: true

logging:
  level:
    org:
      springframework:
        web: DEBUG
